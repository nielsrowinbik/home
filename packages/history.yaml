history:
  exclude:
    domains:
      - input_datetime
      - input_number
      - persistent_notification

logbook:
  exclude:
    domains:
      - group
      - persistent_notification
    entities: []

recorder:
  exclude:
    domains:
      - automation
      - input_number
      - persistent_notification
      - script
      - sun
      - weather
    entities:
      - binary_sensor.today_is_a_national_workday
      - binary_sensor.today_is_a_workday
      - binary_sensor.tomorrow_is_a_national_workday
      - binary_sensor.tomorrow_is_a_workday
