group:
  all_scripts:
    entities:
      - script.scene_sync
      - script.input_sync
      - script.scene_selection_reactivate
      - script.reload_themes
      - script.reset_volume
    icon: mdi:brightness-6
    name: Routines